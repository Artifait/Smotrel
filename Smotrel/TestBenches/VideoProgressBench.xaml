<Window x:Class="Smotrel.TestBenches.VideoProgressBench"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Smotrel.TestBenches"
        xmlns:controls="clr-namespace:Smotrel.Controls"
        Title="Smotrel Demo" Width="600" Height="220"
        Background="#1E1E1E" Foreground="White">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="🎚️ Volume controls Video Progress"
                   FontSize="18"
                   HorizontalAlignment="Center"
                   Foreground="#FFCC00"
                   Margin="0,0,0,12"/>

        <StackPanel Grid.Row="1" Orientation="Vertical" HorizontalAlignment="Center" Margin="0,0,0,12">
            <controls:VolumeSlider x:Name="VolumeCtrl" Height="24" Width="400" HorizontalAlignment="Center" Margin="0,0,0,12"/>
            <controls:VideoProgressSlider x:Name="ProgressCtrl" Height="28" Width="400" Maximum="1" HorizontalAlignment="Center"/>
        </StackPanel>

        <TextBlock Grid.Row="2"
                   x:Name="DebugText"
                   Margin="0,12,0,0"
                   HorizontalAlignment="Center"
                   Foreground="LightGray"
                   FontSize="14"
                   Text="Waiting for interaction..."/>
    </Grid>
</Window>
