<Window x:Class="Smotrel.DialogWindows.AddCourseWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Add Course" SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        MinWidth="480">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <Style x:Key="CapBtn" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="RootBorder" Background="{TemplateBinding Background}">
                            <ContentPresenter Content="{TemplateBinding Content}" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="RootBorder" Property="Background" Value="#44ffffff"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ExitBtn" TargetType="Button" BasedOn="{StaticResource CapBtn}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="RootBorder" Background="{TemplateBinding Background}" CornerRadius="0 10 0 0">
                            <ContentPresenter Content="{TemplateBinding Content}" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="RootBorder" Property="Background" Value="#E81123"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="#1e1e1e" BorderThickness="1" BorderBrush="#3A3A3A" CornerRadius="10">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="64"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" MouseLeftButtonDown="Header_Down" Background="#2d2d2d" CornerRadius="9,9,0,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="40"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" Text="Add Course"
                               VerticalAlignment="Center" Margin="10,0,0,0" FontFamily="Rockwell"
                               Foreground="#AAAAAA" FontSize="18" FontWeight="SemiBold"/>

                    <Button Grid.Column="2" IsCancel="True" Width="40" Content="&#xE8BB;" FontFamily="Segoe MDL2 Assets" Foreground="White" Click="CloseButton_Click" Style="{StaticResource ExitBtn}"/>
                </Grid>
            </Border>

            <Grid Grid.Row="1" Margin="12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Grid Grid.Column="0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="Укажите путь до желаемого курса" Foreground="#CCCCCC" FontSize="14" Margin="6,6,6,10"/>

                    <Grid Grid.Row="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="100"/>
                        </Grid.ColumnDefinitions>

                        <TextBox x:Name="PathTextBox" Text="{Binding SelectedFolderPath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" IsReadOnly="True" VerticalAlignment="Center" Height="34" Margin="0,0,8,0" Background="#262626" Foreground="#EAEAEA" BorderBrush="#3A3A3A" BorderThickness="1" Padding="8"/>

                        <Button Grid.Column="1" Content="Обзор..." Click="BrowseButton_Click" Height="34" VerticalAlignment="Center" Style="{StaticResource CapBtn}" Background="#2b2b2b" Foreground="#EAEAEA" BorderBrush="#3A3A3A" BorderThickness="1"/>
                    </Grid>

                    <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,12,0,0" VerticalAlignment="Center">
                        <TextBlock Text="Название курса:" VerticalAlignment="Center" Foreground="#CCCCCC"/>
                        <TextBox Margin="6,0,0,0" Text="{Binding CourseTitle, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" VerticalContentAlignment="Center" Width="360" Height="30" Background="#262626" Foreground="#EAEAEA" BorderBrush="#3A3A3A" BorderThickness="1" IsEnabled="{Binding IsStatsVisible}"/>
                    </StackPanel>

                    <Grid Grid.Row="3"/>

                </Grid>

                <Border Grid.Column="1" Background="#232323" CornerRadius="6" Padding="14" Margin="16,0,0,0" Visibility="{Binding IsStatsVisible, Converter={StaticResource BoolToVis}}" MinWidth="200">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Text="Статистика курса" FontSize="16" FontWeight="SemiBold" Foreground="#FFFFFF" Margin="0,0,0,12"/>

                        <Grid Grid.Row="1" Margin="0,0,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0" Orientation="Vertical">
                                <TextBlock Text="Видео:" Foreground="#BBBBBB"/>
                                <TextBlock Text="Глав (включая подглав):" Foreground="#BBBBBB"/>
                                <TextBlock Text="Глубина (max):" Foreground="#BBBBBB"/>
                            </StackPanel>

                            <StackPanel Grid.Column="1" Orientation="Vertical" HorizontalAlignment="Right">
                                <TextBlock Text="{Binding CourseStats.TotalVideos}" FontWeight="Bold" Foreground="#FFFFFF" HorizontalAlignment="Right"/>
                                <TextBlock Text="{Binding CourseStats.TotalChapters}" FontWeight="Bold" Foreground="#FFFFFF" HorizontalAlignment="Right"/>
                                <TextBlock Text="{Binding CourseStats.MaxDepth}" FontWeight="Bold" Foreground="#FFFFFF" HorizontalAlignment="Right"/>
                            </StackPanel>
                        </Grid>
                    </Grid>
                </Border>
            </Grid>

            <Border Grid.Row="2" Background="Transparent" Padding="10" CornerRadius="0,0,10,10">
                <Button x:Name="AddButton" Content="Добавьте путь до курса" Height="44" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Click="AddButton_Click" IsEnabled="{Binding IsAddEnabled}" FontSize="16" FontWeight="SemiBold" BorderThickness="0" >
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="#111111"/>
                            <Setter Property="Foreground" Value="#f2fef4"/>
                            <Style.Triggers>
                                <Trigger Property="IsEnabled" Value="True">
                                    <Setter Property="Background" Value="#007ACC"/>
                                    <Setter Property="Foreground" Value="White"/>
                                    <Setter Property="Content" Value="Добавить"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </Border>
        </Grid>
    </Border>
</Window>
